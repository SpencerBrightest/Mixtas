import Link from 'next/link'
import { ArrowRight, Heart, Star } from 'lucide-react'
import { Breadcrumbs, Footer, ProductActions, ProductGrid, SiteHeader } from '@/components/store'
import { getProduct, money, relatedProducts } from '@/lib/catalog'

export default async function ProductPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const product = getProduct(id); return <><SiteHeader /><main className="page-shell"><Breadcrumbs current={product.name} /><section className="product-detail"><div className="gallery"><div className="gallery-main"><img src={product.image} alt={product.name} /></div><div className="gallery-note">Designed in New York / Made for everywhere</div></div><div className="product-detail-copy"><p className="eyebrow">{product.category}</p><h1>{product.name}</h1><div className="rating"><span><Star size={13} fill="currentColor" /><Star size={13} fill="currentColor" /><Star size={13} fill="currentColor" /><Star size={13} fill="currentColor" /><Star size={13} /></span> 4.8 / 5</div><p className="detail-price">{money(product.price)}</p><p className="detail-description">{product.description} Cut for an easy silhouette and finished with the small details that make a piece feel like yours.</p><div className="option"><label>Color</label><div className="swatches">{product.colors?.map((color) => <button key={color} aria-label={color} className="swatch"><span />{color}</button>)}</div></div><ProductActions product={product} /><div className="accordions"><details open><summary>Details & fit</summary><p>Relaxed fit. Please choose your usual size. Model is 5&apos;11&quot; and wears a size M.</p></details><details><summary>Shipping & returns</summary><p>Complimentary shipping over $150. Returns accepted within 30 days.</p></details></div></div></section><section className="related section"><div className="section-heading left"><p className="eyebrow">You may also like</p><h2>Complete the look</h2></div><ProductGrid items={relatedProducts(product.id)} /></section></main><Footer /></> }
