// Customer login page for accessing member accounts.

import { AccountPage } from '@/app/secondary-routes'

/** Renders the customer login page. */
export default function LoginPage() {
  return <AccountPage />
}
