// Customer sign-up page for creating store accounts.

import { AccountPage } from '@/app/secondary-routes'

/** Renders the customer registration page. */
export default function SignUpPage() {
  return <AccountPage />
}
